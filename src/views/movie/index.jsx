import React from 'react';
import Navigation from '../../components/navigation';

const Movie = () => {
    return (
        <div>
            <Navigation />
        </div>
    );
};

export default Movie;